# Realtime Translation Engine

Reusable event-driven translation engine with:

- `TranslationCore`
- `ReplayRunner`
- `LiveRunner`

## Local Development

Run tests:

```bash
PYTHONPATH=src python3 -m unittest tests.test_core tests.test_live_runner tests.test_source_state
```
